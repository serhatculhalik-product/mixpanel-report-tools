{
  "manifest_version": 3,
  "name": "Mixpanel Report Tools",
  "version": "1.0.0",
  "description": "Transpose Mixpanel tables in one click (keeps the native look), copy as TSV, and show % change against a min/max baseline on metric cards.",
  "permissions": ["scripting", "activeTab"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js"
  },
  "action": {
    "default_title": "Mixpanel Report Tools"
  }
}
